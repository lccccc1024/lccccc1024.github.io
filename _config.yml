title: 闲话 # name of the site
author: lccccc1024 # name of site's author
email: asa.lu@outlook.com # email of site's author
url: https://lccccc1024.github.io # root address of the site
baseurl:  # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
  正经人也写日记...
lang: zh-CN
permalink: /:year/:month/:day/:title/ # customize permalink structure, e.g. "/:year/:month/:day/:title/" (default is "/:categories/:year/:month/:day/:title/")

# permalink: 

favicon: "lccccc1024.png" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics

remote_theme: riggraz/no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: ".." # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: :compressed

jekyll-minifier:
  remove_comments: true
  collapse_whitespace: true
  minify_css: true
  minify_javascript: true
  minify_html: true

# 社交SEO配置（新增）
twitter:
  card: summary
og_image: /lccccc1024.png

# 插件配置（新增/修改）
plugins:
  - jekyll-minifier
  - jekyll-sitemap
  - jekyll-feed
  - no-style-please

# 压缩插件配置（新增）
jekyll-minifier:
  remove_comments: true
  collapse_whitespace: true
  minify_css: true
  minify_javascript: true
  minify_html: true

# 代码高亮配置（新增，为后续做准备）
markdown: kramdown
highlighter: rouge
kramdown:
  syntax_highlighter_opts:
    block:
      line_numbers: false
