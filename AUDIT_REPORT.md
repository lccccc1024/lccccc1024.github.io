# 项目修复后审查

日期：2026-09-09。基线提交：dd087e1。本次变更尚未提交或推送。

## 结论

上轮列出的 10 项问题、依赖漏洞及 4 项补充问题已完成本地修复。修复后再次审查源码、生产产物和关键浏览器流程，在本次检查范围内未发现阻塞发布的问题。

## 修复对应

| 上轮问题 | 修复 |
| --- | --- |
| 地图 DOM XSS | 校验数组、字段及坐标范围，限制记录数；名称、日期、备注使用文本节点；删除未使用的旧地图脚本。 |
| Pagefind 加载错误 | 改为动态 ES module 导入，处理失败、输入变化及过期搜索结果。 |
| 页面切换后交互失效 | 使用标准页面导航，移除 ClientRouter 和跨页动画；每个新文档独立初始化。 |
| 图片缺失与离线安装失败 | 将图标放入 public，修正 MIME 和 manifest 中的真实尺寸。 |
| 缓存长期不更新 | 构建后按产物内容生成缓存版本；在线网络优先，离线读取缓存；清理旧本站缓存；不缓存失败响应。 |
| 动画与监听器累积 | 跨页面完整导航释放旧文档；修复粒子初始时间戳，遵循减少动态效果偏好。 |
| 随机文章构建时固定 | 新增静态随机页面，每次访问由浏览器选择文章；无 JS 时提供文章列表。 |
| 日期排序错误 | 先识别 ISO 日期，再比较数字和文本，支持键盘排序。 |
| 代码复制和语言标签失效 | 匹配 Astro 实际的 pre.astro-code 和 data-language。 |
| 搜索失败被 CI 吞掉 | 移除失败兜底 echo，构建索引失败直接阻止后续部署。 |
| 依赖漏洞 | 更新锁文件；实际 Astro 7.3.2、Sharp 0.35.4；npm audit 为 0。 |
| robots.txt 未发布 | 添加 public/robots.txt。 |
| sitemap.xml 不是 XML | 输出真正的 XML sitemap index。 |
| 热力图星期错位、小屏溢出 | 按列排列每周 7 天，补齐首周空格；热力图区域可横向滚动。 |
| 无测试、类型和 PR 检查 | 新增类型配置、9 项 Playwright 回归测试和 PR 校验；验证 job 仅读权限，部署 job 单独写权限。 |

## 验证证据

- `npm run check`：26 个文件，0 errors / 0 warnings / 0 hints。
- `npm run build:all`：Astro 生产构建、Pagefind 索引及缓存版本生成成功。
- `npm test`：9 passed，Chrome 无界面浏览器；包含桌面与 390px 手机宽度场景。
- `npm audit --json`：0 vulnerabilities，包含开发依赖。
- `git diff --check`：无空白错误。
- 扫描全部 54 个 HTML 产物中的站内绝对路径 href/src：无缺失目标。

浏览器用例：资源和 XML 路由、首页/404/命令面板搜索、跳转后的手机菜单与复制/阅读进度、日期升降序、地图恶意输入、不同随机输入产生不同文章、热力图星期与手机页面宽度、Service Worker 安装/在线更新/离线访问、全站本地资源链接检查。

## 审查边界与行为变更

- 为修复生命周期问题，已移除跨页过渡动画，页面使用浏览器正常导航；页内动画和主题切换仍保留。
- 离线只能访问缓存过的页面；未缓存页面会明确提示联网重试，不再冒充首页。
- 地图安全测试替代了高德 SDK，以隔离并验证不可信输入处理。未验证高德账户权限、配额、真实地图服务可用性或 Giscus 账户设置。
- 未逐条访问文章中的外部链接，也未运行远端 GitHub Actions。本地通过不能替代首次推送后的 CI 结果。
- 未部署、未推送；线上仍保持原版本。

## 后续维护

使用 `npm run verify` 完成类型检查、完整生产构建和浏览器测试。首次测试需安装 Chrome，或运行 `npx playwright install chrome`。发布必须使用 `npm run build:all`，确保搜索索引及缓存版本一并生成。
